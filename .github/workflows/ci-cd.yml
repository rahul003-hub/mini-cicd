name: PHP CI/CD

on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: docker build -t rahulhub1313/mini-cicd-php .

      - name: Run container in background
        run: docker run -d --name php-container -p 8080:80 rahulhub1313/mini-cicd-php

      - name: Show all containers (including stopped)
        run: docker ps -a

      - name: Show container logs (initial)
        run: docker logs php-container || true

      - name: Validate container response from inside
        run: |
          sleep 3
          docker exec php-container curl -s http://localhost | grep "Hello from PHP"

      - name: Show container logs (after validation)
        run: docker logs php-container || true

      - name: Push Docker image
        run: docker push rahulhub1313/mini-cicd-php
